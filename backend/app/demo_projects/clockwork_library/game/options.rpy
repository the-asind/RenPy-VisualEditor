# Options-only file. The ProjectGraph canvas should show it as clickable text.

define config.name = "Clockwork Library Demo"
define config.version = "0.1"
define config.save_directory = "clockwork-library-demo"

default preferences.text_cps = 34
default preferences.afm_time = 15
